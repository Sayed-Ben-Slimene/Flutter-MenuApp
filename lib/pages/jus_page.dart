import 'package:flutter/material.dart';

class JusPage extends StatelessWidget {
  const JusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
