import 'package:flutter/material.dart';

class GateauPage extends StatelessWidget {
  const GateauPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
